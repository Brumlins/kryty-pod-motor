<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 */
?>
<?= $this->doctype() ?>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <?= $this->headTitle('Laminas MVC Skeleton')->setSeparator(' - ')->setAutoEscape(false) ?>

        <?= $this->headMeta()
            ->appendName('viewport', 'width=device-width, initial-scale=1.0')
            ->appendHttpEquiv('X-UA-Compatible', 'IE=edge')
        ?>

        <?= $this->headLink([
                'rel' => 'shortcut icon',
                'type' => 'image/vnd.microsoft.icon',
                'href' => $this->basePath() . '/img/favicon.ico'
            ])
            ->prependStylesheet($this->basePath('css/style.css'))
            ->prependStylesheet($this->basePath('css/bootstrap.min.css')) ?>

        <?= $this->headScript() ?>
    </head>
    <body>
        
        <div class="container">
            <?= $this->content ?>
            <hr>
            <footer>
                <p>
                    &copy; <?= date('Y') ?>
                    Projekt pro Extradeon - Tomáš Řehulek
                </p>
            </footer>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/vue@2.6.14/dist/vue.js"></script>
        <script>
            if (document.getElementById('app')) {
                new Vue({
                    el: '#app',
                    data: {
                        search: '<?= isset($filters['search']) ? $this->escapeJs($filters['search']) : '' ?>',
                        selectedBrand: <?= isset($filters['znacka_id']) ? (int)$filters['znacka_id'] : 0 ?>,
                        selectedMaterial: <?= isset($filters['material_id']) ? (int)$filters['material_id'] : 0 ?>
                    },
                    methods: {
                        applyFilters() {
                            document.getElementById('filter-form').submit();
                        }
                    }
                });
            }
        </script>

    </body>
</html>
